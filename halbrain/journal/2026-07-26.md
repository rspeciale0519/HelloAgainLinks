# 2026-07-26

### [12:47] Diagnosed "Up to date" sync + fixed 3 timestamp/honesty bugs; corrected two of my own wrong claims (branch: fix/sync-honesty-and-post-timestamps · commit d875631)
**Synopsis:** User topped up X credits ($25 + $25 spend cap), tapped Sync Now, got "Up to date — no new bookmarks" despite believing they had new saves. Diagnosed via the X Developer Console + prod DB. Sync was working; three real bugs found and fixed. Two claims I made earlier in the session turned out to be wrong and are corrected here.
**What worked + Evidence:** `tsc --noEmit` exit 0.
- **Diagnosis method that paid off:** checked X console Usage FIRST rather than guessing. It showed **Total Requests 4, Total Cost $0.30, one event kind "Read"** — which immediately killed my leading hypothesis (silent token failure), because X had clearly been reached. Then queried prod DB: `stop_reason=caught_up`, `newestKnownPostId=2081079567800774707`, **1519 bookmarks** (296 via api / 854 via extension), newest stored post published 2026-07-25 20:57 via api. So X returned bookmarks that were all already in HAL → `inserted 0`, `skipped` large → the UI's "no new bookmarks" was technically true but read as a failure.
- **Bug 1 — every mobile card showed "1m".** `mobile/home` and `mobile/bookmarks` render `timeAgo(bm.bookmarked_at)`; the sync sets `bookmarked_at: new Date()` at ingest, and `utils.ts timeAgo` returns the **literal string `'1m'`** when `mins < 1` in short mode. So after any sync every row reads "1m". Fixed: cards now use `post_created_at || bookmarked_at`. `/api/bookmarks` already selects `'*'` so no API change; added `post_created_at?: string | null` to both mobile `Bookmark` interfaces.
- **Bug 2 — re-sync clobbered ingest time.** `bookmarked_at` was listed in `mergeUpsertBookmarks` `WRITABLE_FIELDS`, so any merge (incoming score > existing) overwrote it with the incoming "now". Removed it; kept `post_created_at` writable since that's real X data.
- **Bug 3 — sync message hid `skipped`.** `mobile/more/settings` reported only `imported`, so "checked 300 already-saved posts" and "X returned nothing" produced identical text. Now distinguishes them. Same honesty defect class as the 07-20 402 fix, in the same flow.
**What did NOT work:** **Two of my own claims this session were wrong and I corrected them to the user.** (a) I asserted "production is still running the old sync code" from arithmetic (300 resources = 3 pages × 100 = maxStalePages 3). Vercel deployments page shows `876b6a1` / main / **Production / Ready** — the deploy DID happen. (b) Off the back of that I had declared the `expansions=author_id` billing question **settled** ("$0.30 = exactly 300 × $0.001, single Read kind → no separate billing"). With prod on the fix, the sync was incremental at 10/page ⇒ ~40 resources ⇒ should be **$0.04, not $0.30**. The 7.5× gap is instead *consistent with* expansions billing separately (~40 posts × $0.001 + ~26 authors × $0.010 = $0.30). Event **kind** ≠ billing breakdown — I over-read one signal. **The expansions question is OPEN.**
**Artifacts:** commit `d875631` on `fix/sync-honesty-and-post-timestamps` (4 files) — not pushed, no PR yet. X account now funded $25 with a $25 billing-cycle spend cap set (hard financial backstop beneath the app-level quotas).
### [13:45] Released #20/#21; second sync proves page-size fix works; found the mobile-bundle staleness trap
**Synopsis:** Pushed/merged/released the fixes (PR #20 → develop, PR #21 → main `f11634b`). User tapped Sync Now again: got the OLD message text and no visible balance change. Diagnosed — the sync is now working *correctly and cheaply*; the stale message is a mobile-bundle issue, not a code issue.
**What worked + Evidence:** X console after the sync: **Total Requests 4 → 5** (exactly one request) with `stop_reason=caught_up`, and prod DB `lastSyncAt=2026-07-26T17:35:41` (updated ⇒ the loop ran to completion, so NOT one of the silent early returns). Previous sync was 3–4 requests at 100/page = 300 resources; this one was **1 request at 10/page**. **The page-size fix is working as designed — ~30× fewer resources per routine sync.** Token healthy (`expires 18:34`, not expired, refresh token present).
- **KEY REALIZATION — the mobile app is a baked native bundle.** `apps/web/src/app/mobile/**` is statically exported by `scripts/mobile-build.mjs` and shipped inside the Capacitor/TestFlight binary; only `/api/*` runs on Vercel. So a Vercel deploy CANNOT update mobile UI. That is why the user still saw "Up to date — no new bookmarks" instead of the new "checked N, all already saved" text. **The same applies to the "1m" timestamp fix — it will not appear until a new Codemagic build ships.** This trap will recur for every mobile UI change; worth a skill.
- **User's original complaint explained:** `caught_up` means `newestKnownPostId` was present in X's 10 newest bookmarks ⇒ X's API is not returning anything newer than what HAL already has. So the "missing" recent saves are not visible in X's bookmarks endpoint at all — an X-side issue, not a HAL bug.
**What did NOT work:** I stated mid-turn that "that sync cost exactly $0.00" based on balance $24.70 and cost still reading $0.30. **That was premature** — request count updates in near-real-time but cost aggregation lags, so the charge for request #5 likely had not posted. Correct statement: requests +1, cost not yet reflected. Also: the expansion A/B is still NOT run — the user tapped Sync Now before setting `SYNC_INCLUDE_AUTHOR_EXPANSION=false`, so this sync still included expansions. It does give a cleaner baseline though (1 request, ~10 bookmarks, expansions ON).
**Artifacts:** PR #20 merged → develop; PR #21 merged → main (`f11634b`); PR #19 **closed** as superseded — I had branched the fix branch off `feat/max-tier-upgrade-ui` instead of `develop`, so #20's squash carried the Max UI in with it and merging #19 would have reverted the newer fixes. Branch `feat/max-tier-upgrade-ui` deleted.
**Next:** (1) trigger a **Codemagic build** so the mobile UI fixes (timestamps + honest sync message) actually reach the device; (2) re-check X console cost in a few minutes for request #5's charge — that is the expansions baseline; (3) then run the A/B with `SYNC_INCLUDE_AUTHOR_EXPANSION=false`; (4) fix the three silent early returns in `syncUser` (still unfixed — they would report success on a token failure).

### [14:25] Silent-auth-failure fix shipped; Codemagic build #16 green; auto-trigger is NOT working
**Synopsis:** Fixed the three silent early returns in `syncUser`, merged to develop (PR #22 → `1bbffcf`), and the user manually started Codemagic build #16, which produced an .ipa from the right commit.
**What worked + Evidence:** `tsc --noEmit` clean. Build #16: branch `develop`, commit **1bbffcf**, workflow "HAL iOS — TestFlight (internal)", 3m10s on Mac mini M2, artifact `App.ipa` (1.66 MB). All eight steps ran (deps 6s, web build + Capacitor sync 17s, .ipa 52s, **Publishing 1m 13s**). Overall status "finished with post-processing failed" — matches the documented-benign pattern from build #11 ([[journal/2026-07-20]]): Publishing succeeds, and the failure is Apple's separate App Store *distribution* step, irrelevant to TestFlight. Fix itself: each early return now `console.error`s and returns a typed `blocker` ('x_not_connected' | 'x_reauth_required'), passed through by the route's existing `...result` spread and surfaced by mobile settings as "sign in with X again".
**What did NOT work:** **My claim that pushing to `develop` auto-triggers Codemagic was WRONG in practice.** `codemagic.yaml` does declare `triggering: events:[push], branch_patterns:[develop]`, but the dashboard showed "Last build: 7 days ago (Jul 20)" despite pushes to develop on Jul 23, 24 and twice on Jul 26. The webhook is evidently not firing — builds must be started manually until that's fixed. I asserted the trigger worked from reading config + old journal notes rather than verifying. Also: Codemagic's SPA renders empty under CDT automation when loaded cold (title `null - Codemagic`, zero innerText) — it only became readable once the user had it open and interacted with; and clicking an individual build step to expand its log did not stick, so I could not read the Publishing log line-by-line (conclusion rests on step duration + absence of any other error + the build-#11 precedent).
**Artifacts:** PR #22 merged → develop (`1bbffcf`). Codemagic build #16 (id 6a664f92bd2b7b761ed415e9). `main` now BEHIND develop by `1bbffcf` — API-side error logging/blocker responses are not in prod yet.
**Next:** (1) confirm build #16 in TestFlight, install, then **clean sign-out → sign-in** (stale-shell trap, has bitten twice); (2) verify on device: cards show real post dates not "1m", sync message reads "checked N, all already saved"; (3) release develop→main for the API half; (4) **fix the Codemagic webhook** so pushes build automatically again; (5) expansions A/B still unrun.

### [14:51] Build #16 confirmed good on device; absolute post timestamps (branch: develop · commit d844be3 · PR #23)
**Synopsis:** User installed build #16 — all fixes took effect. They then asked to replace relative time counts ("54d", "26w", "1m") with actual dates, and mid-task refined it: compact on mobile, long on desktop web.
**What worked + Evidence:** `tsc --noEmit` exit 0. Formatter verified by direct execution against `dist`: short → `3:04PM - 7/22/26`, long → `3:04 PM - July 22, 2026`, invalid input → `""`. New `formatPostDate(dateStr, {short?})` in `packages/shared/src/utils.ts`; mobile home + bookmarks use short. **Desktop web already had its own date layer** (`packages/ui/hal/src/feed/format-date.ts`, whose header comment states the package deliberately stays decoupled from the web app's helpers, and `ui-hal` has no dependency on `shared`) — so I duplicated the formatter there rather than adding a cross-package dep: `Card.tsx` "Posted …" now long, `Card.grid.tsx` short. The grid card also silently had the SAME ingest-time defect (`formatRelative(bookmarked_at)` → "1s ago" for everything post-sync) and now reads `post_created_at || bookmarked_at`. Card headers got `textOverflow: ellipsis` on the handle plus `nowrap`/`flexShrink: 0` on the date so a long handle can't wrap or displace the timestamp.
**What did NOT work:** Ran `packages/shared` build chained with the web type-check in one Bash call and hit the 2-minute default timeout — split them and raise the timeout for `tsc` on this repo (web type-check alone takes well over a minute). Also hit the `packages/shared` **dist-rebuild gotcha again** (3rd time this session): editing `src/utils.ts` isn't visible to `apps/web` until `npx tsc` runs in that package.
**Artifacts:** PR #23 merged → develop (`d844be3`). Left untouched: `Card.tsx:213` "Saved {formatRelative(bookmarked_at)}" — still relative on ingest time, but it is explicitly labelled "Saved" so it is not misleading.
**Next:** (1) start a **manual** Codemagic build off develop (auto-trigger still broken) to get the new timestamps onto the device; (2) release develop→main — main is now behind by `1bbffcf` + `d844be3`, so neither the API blocker responses nor the new timestamps are in prod web; (3) fix the Codemagic webhook; (4) expansions A/B still unrun.

### [15:51] Green→blue accent revert; released everything to main (branch: main · commit 5407738 · PRs #24, #25)
**Synopsis:** User asked to revert the green accent back to blue everywhere. Done, merged to develop, started a Codemagic build, and released develop→main. main==develop again.
**What worked + Evidence:** `tsc --noEmit` exit 0; grep for `a1fc2a|86d91f|d4ff3a|161,252,42|212,255,58` across all source dirs returns **empty**. Only 4 files needed changing because `772604f` had routed everything through tokens: `apps/web/src/app/globals.css` (`--accent-cyan #00d4ff`, `--accent-blue #0ea5e9`, `--accent-rgb 0,212,255`), `packages/ui/hal` globals.css + theme.ts (`--hal-a` #00d4ff + rgb/dim/glow), `packages/ui/src/HalLogo.tsx` (gradient + glow). Recovered exact original values from git (`git show 772604f^:<file>`, `git show d3c08c2^:<file>`) rather than guessing. Rebuilt `@helloagain/ui` because it resolves from `dist` — verified `dist/HalLogo.js` carries `#00d4ff` (this exact trap left the logo stale on 07-20).
- **Ambiguity resolved and flagged to the user:** "revert to the original blue" is not literally possible app-wide — pre-migration the dashboard (`ui-hal`) was `#d4ff3a`, **also a green**; only mobile/landing was blue. A literal per-system revert would have left the dashboard green, defeating the request, so both systems were unified on `#00d4ff` and the user was told explicitly so they can correct it cheaply.
- **Codemagic**: started build `6a66650489a28db59c44e14e` from the modal via CDT (radio "Build branch" + branch `develop` + iOS workflow; SSH/VNC left off). The modal exposed that the prior build `6a665b23…` was for commit **d844be3** — i.e. timestamps but NOT the blue revert — confirming a fresh build was genuinely needed rather than assumed.
- Released PR #25 → main `5407738`; `git log origin/main..origin/develop` now empty (parity).
**What did NOT work:** Codemagic's build-overview metadata block (ID/Index/Status/Branch/Commit) would not render under CDT on that page even after reload — only the step list did. Worked around it by opening the Start-new-build modal, which *did* render and revealed the prior build's commit. Lesson: on Codemagic, the modal is more reliably readable than the overview panel.
**Artifacts:** PR #24 → develop (`379d5a9`), PR #25 → main (`5407738`). Codemagic build `6a66650489a28db59c44e14e` running off develop.
**Next:** (1) confirm the new build in TestFlight → clean sign-out/sign-in → verify blue accent + `3:04PM - 7/22/26` timestamps; (2) Vercel will auto-deploy `5407738` for web (blue accent + API blocker responses now in prod); (3) still open: Codemagic webhook broken (auto-trigger), expansions A/B unrun, `UpgradePrompt.tsx` + mobile settings lack the Max tier.

### [16:05] Diagnosed the broken Codemagic auto-trigger (no code change)
**Synopsis:** Investigated why pushes to `develop` stopped triggering Codemagic builds after Jul 20. Narrowed it to the GitHub↔Codemagic event path; final confirmation blocked by GitHub sudo re-auth.
**What worked + Evidence:** Repo **Settings → Webhooks is empty** — zero webhooks on `rspeciale0519/HelloAgainLinks`, so Codemagic is not using a classic repo webhook. Repo **Settings → Integrations → GitHub Apps** shows **Codemagic CI/CD (NevercodeHQ) installed**, installation id **147652362**. So the app exists but no push events are reaching Codemagic.
**What did NOT work:** Could not open `github.com/settings/installations/147652362` to read the app's repository-access scope — GitHub demanded sudo-mode re-authentication (passkey/password). Correctly stopped rather than authenticating on the user's behalf. Note a complication for the leading hypothesis ("app not scoped to this repo"): **manual builds clone the repo fine**, so Codemagic clearly has read access — meaning source access and event delivery may run through different mechanisms (stored OAuth/SSH for cloning vs the GitHub App for events), or the app is scoped correctly and the fault is on Codemagic's side (app-level automatic-triggering setting / stale event subscription).
**Artifacts:** none — investigation only.
**Next:** user to (1) check Codemagic → app → Settings for webhook/automatic-trigger status and re-register if offered; (2) check installation 147652362 repository access includes HelloAgainLinks; (3) failing both, disconnect+reconnect the repo in Codemagic to re-register event subscriptions. **Low priority** — manual builds work and this is pre-launch with one dev.

### [17:01] Evaluated X Activity API + X MCP for cost; found the 24h dedup rule that reframes the whole X cost model (no code change)
**Synopsis:** User asked whether the two X Developer Console announcements (X MCP, X Activity API) would be useful and save operating cost. Answer: the announcements themselves, no — but investigating them surfaced two genuinely important billing facts.
**What worked + Evidence:**
- **X Activity API is definitively useless for HAL.** Enumerated the Create-Subscription form exhaustively by driving the native `<select id="create-event-category">` through every value and reading `#create-event-type` (React-controlled, so used the native value setter + `change` event). Full event catalogue: **Profile** (Bio/Profile Picture/Banner/Screenname/Geo/URL/Verified Badge/Affiliate Badge/Handle Update), **News** (New News), **Chat** (Received/Sent/Conversation Join), **DM** (Sent/Received/Typing/Read), **Follow** (Follow/Unfollow), **Spaces** (Start/End), **Post** (Create/Delete/Mention Create), **Like** (Like Create). **There is no bookmark event of any kind** — so push can never replace HAL's bookmark polling. Cancelled the dialog; nothing created.
- Activity events are **billed per event delivered** (`dm.received`/`follow.follow` $0.010; `post.create`/`profile.update.*` $0.005), so subscribing would ADD cost.
- **THE FINDING — 24-hour deduplication.** Primary source, docs.x.com/x-api/getting-started/pricing: *"All resources are deduplicated within a **24-hour UTC day window**"* and *"You are charged once per billable event, deduplicated within the same 24-hour UTC window as other resources."* **This explains today's mystery**: requests 5/6/7 cost $0.00 because they re-fetched bookmarks already billed earlier the same UTC day. **X API cost therefore scales with UNIQUE resources per UTC day, not per sync** — frequent syncing is near-free. My earlier ~$15/user/month sync estimate was badly wrong; it assumed every sync bills in full. X calls dedup a "soft guarantee", so it is a discount to expect, not a number to forecast on.
- **xAI credit kickback is a real, usable saving:** up to 20% of cumulative X API spend returned as **xAI credits** ($0–199: 0%, $200–499: 10%, $500–999: 15%, $1,000+: 20%). HAL pays for both X API and Grok, so this directly offsets the AI bill at scale. At $25 cumulative spend it is currently 0%.
- **X MCP** is a developer/agent tool ("connect Grok, Cursor, or any MCP-compatible AI tool to the X API"). Billing is per resource fetched regardless of transport, so routing through MCP would not change spend — marked as inference, not verified against MCP-specific docs.
**What did NOT work:** First two attempts to drive the form failed because the selects are keyed by `id`, not `name` (`document.querySelector('select[name=...]')` returned null while `document.querySelectorAll('select')` clearly showed them) — check both attributes before concluding a control is missing. **Also invalidates my own earlier test plan:** the proposed `SYNC_INCLUDE_AUTHOR_EXPANSION=false` A/B would be **confounded by dedup** — a second sync of the same bookmarks on the same UTC day is free regardless of expansions, so any comparison must run on a fresh UTC day or against distinct resources.
**Artifacts:** none — investigation only. No subscription created.
**Next:** (1) re-derive the per-user cost model around unique-resources-per-UTC-day rather than per-sync; (2) run the expansions A/B only on a fresh UTC day; (3) revisit Activity API only if X ever adds a bookmark event type.

### [19:09] Grok key fixed, model questions settled, mobile UI fixes; released to main (branch: main · commit a6e9ba6 · PRs #26-29)
**Synopsis:** User reported two mobile UI defects plus a failing AI assistant, and asked how to set up the Grok key. Diagnosed the AI failure from prod logs, fixed both UI issues, and — once the user rotated the key — finally answered the model-availability question that had been open since the model switch.
**What worked + Evidence:** `tsc --noEmit` exit 0 on every change.
- **AI assistant failure root cause, from Vercel logs (not guesswork):** `[AI Assistant] Error: Grok API error 400: {"code":"invalid-argument","error":"Incorrect API key provided."}` — the **key**, not the grok-4.5 default I had flagged as a deploy risk. The UI string "Sorry, I had trouble with that" is the client fallback when the response carries no `reply` field, so it masked a server error.
- **After the user rotated `XAI_API_KEY`, verified against the live API:** `GET /v1/models` → 200, and **grok-4.5 and grok-4.3 are both available**; **grok-3 and grok-3-mini are absent entirely** — so the pre-existing defaults would have failed regardless of the key. The model switch was necessary, not merely cheaper.
- **xAI returns authoritative cost:** the usage block contains `cost_in_usd_ticks` at **1 tick = 1e-10 USD**. Verified by hand against the rate card on both models and matched exactly (grok-4.5: 213 prompt/128 cached + 14 out → 2,924,000 ticks = $0.0002924; grok-4.3: 199/128 + 126 out → 4,293,500 = $0.00042935). `estimateCostUsd` now prefers it, falling back to the local table, and logs `cost_source`.
- **Mobile AI composer:** root cause was `height:100%` on the page, which does not resolve inside the layout's scrolling `main`, so the container collapsed to content height and the input rode up under the last message. Composer is now `position:fixed` above the nav (`calc(64px + env(safe-area-inset-bottom))`); `<input>` → `<textarea>` growing to a 5-line cap then scrolling; Enter sends, Shift+Enter newline; message list padded to clear it.
- **Callback card centering:** `minHeight: 100vh` → `100dvh` — on mobile browsers `100vh` counts the area behind the browser chrome, so the grid box exceeded the visible viewport and `placeItems:center` landed the card below centre.
- Consolidated both model tiers on grok-4.5 (constants kept separate so a split is an env change).
**What did NOT work:** **Corrected my own claim mid-turn:** I had said grok-4.3 is "more expensive" based on a single one-word prompt where fixed reasoning overhead dominated (125 reasoning tokens vs 13). That does **not** generalise — on a realistic ~500-in/~120-out classification call 4.3's cheaper rates may still win. The real justification for one model is that the spread is ~$0.06/user/month, i.e. noise; told the user so rather than letting the wrong premise stand. Also: a JSX `{/* */}` comment placed directly after `return (` and before the root element is a parse error, and a `//` comment there containing `<main>` also broke the JSX parser — put such comments above the `return`.
**Artifacts:** PRs #26, #27, #28 → develop; PR #29 → main (`a6e9ba6`); main==develop. Mobile UI changes need a Codemagic build to reach the device.
**Next:** user starts the Codemagic build manually. Then verify on device: composer pinned to bottom + grows to 5 lines, callback card centred, blue accent, absolute timestamps, and the AI assistant actually replying now that Vercel holds a valid key.

### [19:48] Login no longer closes the tab — confirmed working (branch: main · commit 7e5d2be · PRs #30-33 · extension 0.5.2)
**Synopsis:** User reported that signing in via the Chrome extension closed the browser tab entirely. Took three passes to land it; user confirmed working.
**What worked + Evidence:** `tsc --noEmit` clean on `apps/web` and `apps/extension`; rebuilt `dist` verified at 0.5.2 with **zero** occurrences of `tabs.remove`; **user confirmed the flow works**.
- **Root cause (real, and broader than reported):** `background.ts` `onMessageExternal` AUTH_TOKEN called `chrome.tabs.remove(sender.tab.id)` **unconditionally**, and `set-session` did `if (delivered) return` — skipping the `/dashboard` navigation. The `dashboard.ts` content script sets `hal_extension_id` on **every** helloagainlinks.com page, so `set-session` ALWAYS found an extension and always delivered a token ⇒ both halves fired on ordinary web logins too. Mobile was unaffected (different callback, never talks to the extension).
- **Latent bug found and fixed en route:** `clearTokenUrl()` wipes the hash on the first pass, so any second invocation of the set-session effect found no tokens and pushed `/login?error=no_tokens` — dumping an *authenticated* user on an error page. This is the "StrictMode auth flash" already logged in `knowledge/superseded`; it had been invisible only because the tab was destroyed before anyone saw the second pass. Now guarded with `startedRef`. **Consider marking that superseded item resolved at next consolidation.**
- Final shape: nothing closes the tab. `tabs.remove` deleted; `set-session` always continues to `/welcome` or `/dashboard`; `extension-callback` redirects to `/dashboard` instead of `window.close()`. The extension still receives the token in every flow, so it stays signed in.
**What did NOT work:** **Two self-inflicted errors, both worth remembering.**
(a) **Mis-scoped the fix by inferring intent from code.** The user said the *extension* login closes the tab; the code and its copy ("This window will close") read as deliberate, so I decided that flow was correct and fixed only the web path — preserving the exact behaviour being complained about. Shipped, and the user came back with "it's back to closing the tab entirely again." Reading design intent out of existing code is not the same as asking what the user wants.
(b) **Created a broken mixed state by ignoring deploy coupling.** The extension and web app are a matched pair for this handshake. I had the user rebuild/reload the extension while the web half was still committed **locally and unpushed**, so new-extension + old-web produced the confusing `?error=no_tokens`. **Rule: ship the web side first, let Vercel finish, then reload the extension** — the reverse parks the user on `/auth/set-session`.
Also (4th build-output gotcha this session): Chrome loads `apps/extension/dist`, not `src` — bumping `manifest.json` in `public/` changes nothing until `pnpm build` runs. The user reloaded repeatedly and saw the old version number; had the version bumped without a rebuild it would have looked updated while still broken.
**Artifacts:** PRs #30, #32 → develop; #31, #33 → main (`7e5d2be`); extension **0.5.2** (rebuilt dist is local-only, not committed).
**Next:** (1) extension dist must be rebuilt anywhere else it is loaded; (2) at next consolidation, revisit the `set-session` StrictMode item in [[knowledge/superseded]] — now fixed; (3) unrelated and still open: Codemagic auto-trigger, expansions A/B on a fresh UTC day, Max tier missing from `UpgradePrompt.tsx` and mobile settings.

**Next:** (1) push + PR the fixes; (2) **run the controlled expansions test** — strip `expansions=author_id`/`user.fields` from one sync and compare cost per request; if it drops ~7× we should resolve author names from our own DB (we already store `x_author_handle`/`x_author_name`); (3) note the 1519 existing rows keep ingest-time `bookmarked_at` forever — unrecoverable, X's API never reports when you bookmarked something, so `post_created_at` is the only honest display value; (4) PR #19 (Max tier UI) still open; (5) `UpgradePrompt.tsx` + mobile settings still lack the Max tier.
