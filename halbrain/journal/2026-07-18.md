# 2026-07-18

### [00:00] brain initialized (branch: - · commits: none)
**Synopsis:** brain scaffolded from _brain engine.
**What worked:** init. Evidence: manual: brain verify GREEN.
**What did NOT work:** (none)
**Artifacts:** halbrain/*
**Next / open:** run knowledge-build runbook when codebase stable.

### [00:00] knowledge-build runbook, full pass (branch: develop · commits: none)
**Synopsis:** Ran the full knowledge-build runbook end-to-end at user request ("back-fill it with all that has been done in this repo so far") — 4 parallel independent-research agents (product intent, codebase build-status, integrations, doc-vs-code conflicts), then an independent re-verification spot-check on the highest-risk claims (Grok function-calling absence, Blend OG card, extension export, Signal Boards, record-merge wiring), then synthesized all four knowledge docs in the runbook's required order.
**What worked:** Parallel dimension research + independent 2nd-pass re-grep on ~6 sampled claims all corroborated the sub-agent reports with zero contradictions. Evidence: manual: direct Grep re-checks against `grok.ts`, `grok-conversation.ts`, extension source, `StatusDot.tsx`, `set-session/page.tsx`, `bookmark-upsert.ts`; `brain verify` GREEN after synthesis.
**What did NOT work:** (none) — no conflicting evidence found between the 4 independent research passes.
**Artifacts:** `halbrain/knowledge/{features,roadmap,superseded,orientation}.md` flipped stub→current. Classification counts: ~25 BUILT, 1 PARTIAL (AI Assistant — no real function calling), 6 PLANNED (CSV/JSON export, Grok x_search/Collections API, Blend OG card, Signal Boards, The Pulse, Community Knowledge Graphs), 2 UNVERIFIED (Blend privacy controls, blends table schema), 3 integrations NOT FOUND (Supabase Storage/Realtime/pgvector). 6 genuine doc-vs-code conflicts recorded in `superseded.md` (stale MVP checkboxes, PRD mobile self-contradiction, pnpm version, function-calling overclaim, OG-card overclaim, x_search overclaim). 8 operator-flagged tech-debt items re-verified from `claude-memory:project_hal_redesign_open_bugs.md` (2026-04-26) — 6 still present (2 got worse: extension file LOC caps), 2 resolved (SignalPlaceholder archived, ui-hal React peerDep fixed).
**Next / open:** Tag filtering (client-side-only), StrictMode auth flash, and the two oversized extension files remain open tech debt — see `knowledge/superseded.md`. Test-suite coverage and CI/CD/monitoring setup were out of scope for this pass (feature build-status only) and remain unverified.

### [00:00] mobile sync-trigger decision research (branch: develop · commits: none)
**Synopsis:** Evaluated how mobile "native bookmark mirror" sync should be triggered (device background tracking is impossible; it's server-side X-API polling). Recommended app-open + resume user-mode sync over a Vercel cron; checked all assumptions against product docs at user request.
**What worked:** Assumption-check against PRD/roadmap/mobile spec confirmed the recommendation and upgraded it from optimization to necessity: shipped onboarding copy already promises automatic sync (`mobile/onboarding/page.tsx:163,236`) but the only live trigger is a manual settings-page button. Push notifications and offline are explicitly Out of Scope/Future (mobile spec §13); future scheduled features (Scout agent, batch auto-tag) are planned on Supabase Edge Functions + pg_cron, not Vercel Cron (roadmap lines 280, 488) — so nothing forces Vercel Pro. Evidence: manual: read `api/sync/background/route.ts` in full, grepped for cron/notification/scheduler across docs + src, WebFetch of vercel.com/docs/cron-jobs/usage-and-pricing (2026-06-16: Hobby = once/day max, ±59min jitter, more-frequent expressions fail deploy — confirms 15-30min cron needs Pro).
**What did NOT work:** Vercel doc-search MCP tool twice failed to surface the plan-limits table (returned only usage syntax); direct WebFetch of the pricing page worked.
**Artifacts:** `knowledge/superseded.md` delta #7 added (mobile spec §8 falsely claims sync cron "already functional"). Decision (pending user lock-in): app-open + Capacitor `resume` sync with ~2min throttle; skip cron until a user-absent-freshness feature ships or an 800-window loss report appears.
**Next / open:** Implement the app-open/resume trigger if user approves. Cron-mode `POST /api/sync/background` also has a latent scaling flaw (sequential per-user loop, 55s/user guard vs function time limit) — fix before ever enabling cron mode at scale.

### [15:45] feature/mobile-sync-trigger Phase 1 (branch: feature/mobile-sync-trigger · commits: 725011c)
**Synopsis:** Implemented app-open + resume auto-sync per `.claude/plans/feature-mobile-sync-trigger.md` under a /goal run. New `use-auto-sync.ts` hook (native-only, 2-min Preferences throttle, silent unauthenticated skip, absolute APP_URL fetch) wired into the mobile layout.
**What worked:** Implementation itself was small; the discoveries mattered more. Evidence: `pnpm --filter @helloagain/web lint` clean, full `pnpm --filter @helloagain/web build` green, `pnpm mobile:build` green end-to-end (static export + cap sync android/ios), turbo `pnpm lint` 4/4 green at checkpoint.
**What did NOT work:**
- Pre-existing: the manual "Sync Now" button was broken twice over — `fetch` defaulted to GET against a POST-only route (405) AND used a relative URL, unreachable from the native local bundle (`webDir: 'out'`, no `server.url`). API calls from the native shell must be absolute against `NEXT_PUBLIC_APP_URL` (the layout's mobile-session call was the only correct example). Fixed in this phase.
- `pnpm mobile:build` failed deterministically on Windows: EPERM renaming `apps/web/src/app/.well-known` — a process holds a handle on the directory inode itself (children moved fine; empty dir still unrenameable; `attrib -P` clear didn't help). Fixed `scripts/mobile-build.mjs` with a moveDir/restoreDir fallback that moves children and leaves the husk dir (Next ignores empty route dirs).
- _brain engine bug (upstream, NOT fixed here per Rule 11): PostToolUse hook resolves the vault path relative to the shell CWD — Bash commands run with `cd apps/web/src/app` caused it to create a stray `apps/web/src/app/brain/.brainstate/<session>.ledger` with the default vault name. Merged the stray lines into `halbrain/.brainstate/` and removed the stray. Report to `_brain` repo; sync the fix into instances afterward.
**Artifacts:** `apps/web/src/lib/use-auto-sync.ts` (new), `apps/web/src/app/mobile/layout.tsx`, `apps/web/src/app/mobile/more/settings/page.tsx`, `scripts/mobile-build.mjs`, `.gitignore` (+`apps/web/out/`), roadmap Mobile Delivery Track updated with the defer-cron decision. Commit 725011c.
**Next / open:** PR to develop (finish step). Owner must manually verify on device: real resume-triggered sync, throttle behavior, share-sheet unaffected. Upstream _brain CWD bug to report.
