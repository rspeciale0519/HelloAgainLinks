# Journal — 2026-07-31

### [01:30] Full codebase-vs-dev-docs audit + doc reconciliation   (branch: develop · commits: none)
**Synopsis:** Audited the entire codebase against `PRD.md` and
`DEVELOPMENT_ROADMAP.md` via 5 parallel verification passes (extension, AI,
Blend/payments/auth/DB, mobile/sync, HAL dashboard) over `develop` @ `8ac341f`,
then rewrote the docs and the brain knowledge layer to match reality. Docs now
carry a dated audit section with a 13-defect list; knowledge files refreshed to
`updated: 2026-07-31`.

**What worked:** Fanning out 5 read-only audit agents with per-claim
CONFIRMED/CHANGED/WRONG verdicts against the existing evidence-gated
`knowledge/features.md` — the claim-by-claim framing surfaced deltas the 07-24
pass missed (orphaned routes, broken invite links). Evidence: `npm run build`
(turbo: tsc + next build, 4/4 successful) and `npm run lint` (0 errors) both
green; every finding carries file:line citations recorded in
[[knowledge/features]].

**What did NOT work:** `npm run type-check` — no such root script exists (build
is the type gate). The initial claim set (from the 07-24 knowledge pass) was
wrong in places the audit corrected: `/api/ai/*` routes assumed live are
orphaned (live paths: `/api/bookmarks/search`, `/api/bookmarks/[id]/related`,
enrichment `ai_summary`); "no rate limiting / no cost tracking" was stale
(quota.ts + llm-usage.ts + migration 011 exist); auto-sync throttle documented
as 2 min is actually 15 min; set-session StrictMode flash documented as open
was fixed in PR #32; sync 402 "non-200" claim wrong (200 + `xApiError` body).

**Artifacts:** `docs/dev-docs/DEVELOPMENT_ROADMAP.md` (audited checkboxes +
defect list), `docs/dev-docs/PRD.md` (Implementation Status Addendum),
[[knowledge/features]], [[knowledge/superseded]], [[knowledge/roadmap]],
STATE.md; auto-memory `project_hal_redesign_open_bugs` reconciled. Headline
defects found: Blend invite links 404 (no `/blend/**` page), folder-scoped
search ignored server-side, extension side panel unreachable from toolbar, iOS
Share Extension missing though referenced, mobile share response-shape
mismatch, X-sync discards ai_summary/ai_tags, 3 unmetered Grok paths, Stripe
plan-drift on subscription.updated, zero automated tests.

**Next / open:** User to triage the 13-defect list (Blend 404 is
product-breaking); consolidation still owed (skills distillation from journals
07-26→07-31); dead-code sweep and LOC-cap refactors listed in
[[knowledge/roadmap]].

### [03:10] Audit defect sweep — 11 of 13 defects fixed in 4 phases   (branch: bugfix/audit-defects · commits: 899a559, fe00b32, c218e70, e82071d)
**Synopsis:** Worked the 2026-07-31 audit defect list per
`.claude/plans/bugfix-audit-defects.md`. Phases: (1) Blend invite landing page
+ cap-leak close, (2) folder/tag search filters (migration 012) + sync
enrichment + honest counts/sorts, (3) quota on all Grok paths + Stripe plan
re-sync, (4) mobile share contract + extension side panel (0.5.5).

**What worked:** Fixing the mobile share route TO the shipped binary's
expectations (409 + `bookmark` payload) instead of changing the sheet — old
native bundles get the fix with no Codemagic build. Stamping the internal
price_id into Stripe subscription metadata at checkout, with amount+interval
fallback for pre-existing subs — caught that `sub.items[].price.id` is a
Stripe-generated inline-price id that `planForPriceId` can never match.
Evidence: `npx tsc --noEmit` exit 0 per phase; `npm run lint` green per phase
(4/4 turbo tasks); extension `pnpm build` ✓ at 0.5.5.

**What did NOT work:** `/git-workflow-planning:start` mis-parsed its args
twice (type slot got the subject, subject slot got literal `$2`) — did the
branch operation manually per Rule 6 naming. The checkpoint skill's context
ran `git status` in `apps/web` (shell cwd persisted there) and hit a
**previously unknown nested git repo at `apps/web/.git`** (branch `main`,
tracks everything incl. `.next/`) — reported to user, not deleted. Also hit
the `packages/shared` stale-dist trap again immediately after writing the
skill about it ([[skills/build-stale-artifact-traps]] — the skill's check
works; the reflex isn't automatic yet).

**Artifacts:** migration `012_search_folder_tag_filters.sql` (**must be
applied to prod before deploy** — signature change, drop+recreate with 010
ACLs re-asserted), `/blend/invite/[code]/page.tsx`, extension 0.5.5 (dist
rebuilt locally), plan file, roadmap defect list updated per phase.
Deferred: iOS Share Extension target (#6), AskTab-vs-trial gating (#11),
Stripe webhook idempotency store.

**Next / open:** push + PR to develop; user applies migration 012 before
merge-deploy; user reloads extension 0.5.5; nested `apps/web/.git` removal
awaiting user decision.

### [04:30] Blend viral loop completed + legal pages   (branch: feature/blend-share-card · commits: pending)
**Synopsis:** Built the missing half of Bookmark Blend's growth loop — the
1200×630 OG share card (`/api/blends/[id]/card`, next/og ImageResponse) and
the public `/blend/[id]` page (OG/twitter meta → card, Share-on-X intent,
download, create-your-own CTA), linked from dashboard blend rows — plus
`/privacy` and `/terms` pages wired to the login consent line (CWS blocker).

**What worked:** Satori needs TTF, not woff2 — resolved stable Geist Mono TTF
URLs by curling Google Fonts css2 with a plain UA before writing the route,
and cached the ArrayBuffers at module level with a graceful no-font fallback
so a font outage degrades the card instead of 500ing a share preview.
Avatars embedded as base64 data URIs (fetch + try/catch) so a dead
pbs.twimg.com URL can't break the render. Card's signature element encodes
the data: the scan-line between avatars fills to the score. Evidence:
`npx tsc --noEmit` exit 0; lint + `turbo build` green (see checkpoint
commits).

**What did NOT work:** The squash-merge of PR #47 diverged local `develop`
(3 never-pushed commits were content-contained in the squash) — verified
`git diff <branch-tip> origin/develop` was empty before `reset --hard`;
lesson: push develop-side docs commits BEFORE branching planned work off
them, or the finish flow forks history. Also: the auto-mode classifier
blocks rm/mv of `apps/web/.git` — user must remove it manually.

**Artifacts:** `api/blends/[id]/card/route.tsx`, `app/blend/[id]/page.tsx`,
`app/privacy/page.tsx`, `app/terms/page.tsx`, login consent links,
dashboard "VIEW SHARE PAGE →", roadmap/PRD/brain updated.
Four tier themes: ≤25 violet, ≤50 amber, ≤75 cyan, >75 pink.

**Next / open:** PR to develop; visual check of the card against a real
active blend (needs one in prod); remaining Blend gaps (feed, decline,
privacy controls) unblocked but unscheduled.

### [05:40] Migration 012 applied to prod; PR #48 merged (merge commit)   (branch: develop · commits: c878e90)
**Synopsis:** Applied `012_search_folder_tag_filters.sql` to the prod database
via the dashboard SQL editor, verified it landed through the project's
PostgREST endpoint, then merged PR #48 (blend share card + public page + legal
pages) into `develop` with a real merge commit.

**What worked:** Wrapping the drop+recreate in `BEGIN; … COMMIT;` — the first
run died mid-flight with `Error: Failed to fetch` during a live Supabase
incident, and the transaction meant prod was left untouched rather than
functionless. Verifying via `https://<ref>.supabase.co/rest/v1/rpc/` instead of
the dashboard: the project's PostgREST API is a different service from
`api.supabase.com`, so it answered while the control plane was down and gave an
unambiguous answer. Evidence (manual, curl): before — 9-arg `PGRST202`, 7-arg
200 (proved NOT applied); after — 9-arg 200, 7-arg 200 (backward compatible, so
currently-deployed prod code keeps working), anon key `42501 permission denied`
(migration-010 ACL hardening survived the recreate). Pre-flight `pg_proc` read
confirmed prod matched 010 exactly before any change. Distilled into
[[skills/supabase-prod-migration-route]].

**What did NOT work:** The brain's credential note was wrong — tested psql 17
directly: `DIRECT_DATABASE_URL`, `DATABASE_URL` and `SUPABASE_DB_PASSWORD` all
carry the SAME 27-char password and all fail auth, so there is no working
direct-psql/CLI route to prod at all (corrected in [[knowledge/superseded]];
the DB password needs a dashboard reset). Also: `/sql/new` reuses the last
Untitled snippet, so `setValue` silently clobbered a scratch query the user had
there — read-and-restore, done here, but only noticed by checking first.

**Artifacts:** prod `search_bookmarks` now 9-arg (p_folder_id, p_tag_ids),
service_role-only EXECUTE, `search_path=public`. Merge commit `c878e90`
preserving phase commits 03063e8/c7a3273/ce5db54; `feature/blend-share-card`
deleted. New skill + superseded correction. Memory:
`feedback_merge_strategy` (merge commits, never squash).

**Next / open:** develop→main release still needed for any of this to reach
prod web (prod runs `main`; migration 012 is now in place ahead of it, which is
the correct order). Card still unverified visually against a real active blend.
Nested `apps/web/.git` + stray `brain/` dirs still await manual removal
(classifier blocks me).
