# 2026-07-20

### [00:40] Mobile app fully working; then Bookmarks-tab paging bug (branch: develop · commits: d1014d8 · TF builds 10→11)
**Synopsis:** Continuation of the 2026-07-19 mobile X-login debugging chain (see [[journal/2026-07-19]] for the full 6-layer story: crash → X secret → handoff secret → CORS → session storage/API base → fresh-login). After a fresh sign-in on build 10 the app worked end-to-end (bookmarks visible, `/api/bookmarks` 200 in prod logs). Then the user found the **Bookmarks tab** specifically still stuck on "Loading…" while Home worked.
**What worked:** Diagnosed from prod logs, not guesswork — `GET /api/bookmarks 400` (validation error) for the Bookmarks tab vs `200` for Home. Cause: the tab requested `page=0` but `listBookmarksSchema` is 1-indexed (`page: z.coerce.number().int().min(1)`, schemas.ts:40); Home works only because it omits `page` (default 1). On the 400, `fetchBookmarks` hit `if (!res?.ok) return` and never cleared `loading` → permanent spinner. Fix (commit d1014d8): start paging at 1 (`useState(1)`, reset useEffect → `fetchBookmarks(1, true)`; load-more `page+1` now yields 2,3…), plus defensive `setLoading(false)` on failed fetch so an API error shows "No bookmarks found" instead of hanging. Audited the other tabs — blend (`/api/blends`), tags (`/api/tags`), lists (`/api/shared-lists`) use param-less GETs, no page-index bug. Evidence: only Bookmarks used pagination; pushed to develop → Codemagic build #11 (commit d1014d8) started on Mac mini M2 (Codemagic status was "Degraded" so it may run slow).
**What did NOT work:** (none new) — the earlier "build 10 still shows Loading" was NOT a fix failure; the user was on a carried-over logged-in shell from build 9 (session in old cookie store, not the new localStorage store), resolved by a fresh sign-in. See [[journal/2026-07-19]] [20:35].
**Artifacts:** commit d1014d8 (`apps/web/src/app/mobile/bookmarks/page.tsx`), Codemagic build #11 pending → TestFlight. AL-5 secret-warning on the 07-19 journal was a false positive (flagged the ASC Issuer ID UUID, a var NAME, and the Vercel project ID — all non-secret identifiers/pointers, not credentials).
**RESOLVED:** Build #11 in TestFlight (Publishing succeeded; the "post-processing failed" is only Apple's App Store *distribution* step, irrelevant to TestFlight). User did a clean Sign Out → Sign In on build 11; every page now loads. Evidence: prod logs `GET /api/bookmarks 200` (01:05, no more 400) + tags 200. The full mobile app is working end-to-end: launch, X login, session persistence, Home, Bookmarks, and all tabs. NOTE the recurring gotcha that cost time twice: after a client-side session/paging change, an existing logged-in shell is stale — a clean Sign Out → Sign In is required to retest; "logged in" (onboarding_complete) ≠ has-session.
**Still open from 07-19: (1) fold develop-only mobile fixes (abc7b7f, d1014d8) into next develop→main web release for branch parity; (2) PR #9 (feature/mobile-sync-trigger) awaits merge decision; (3) android-release + GH Actions workflows lack env/guard/CORS parity (untested); (4) signOut() doesn't clear onboarding_complete (minor cleanup).

### [01:45] All four loose ends handled (branches: develop→main · commits: 36c3e02, 3ec14ad · PR #12 merge 0e766cc; PR #9 closed · TF build 12)
**Synopsis:** User asked to handle all four loose ends "in order." Did them dependency-sequenced (code/config first, release last) so the parity release captures everything and nothing re-breaks parity.
**What worked:**
- **#4 sign-out (36c3e02):** `signOut()` (mobile/more/page.tsx) now `Preferences.remove('onboarding_complete')` so a signed-out user can't relaunch into a sessionless shell. Chose the SAFE flag-clear over a layout self-heal (adding a getSession gate to the just-fixed login path was too risky given observed getSession flakiness).
- **#3 CI parity (36c3e02):** mobile-build.mjs honors `CAP_PLATFORM` (allowlisted ios|android; unset=sync-all, backward compatible); codemagic android-release → `CAP_PLATFORM=android pnpm mobile:build` (was a bare next build that fails output:export); GH Actions build-ios.yml + release-mobile.yml → `pnpm mobile:build` + CAP_PLATFORM + public NEXT_PUBLIC_* env. CAVEAT: android/GH-Actions corrected by reasoning, still UNVERIFIED (no android device; can't run GH Actions here). Evidence: mobile export compiles+syncs locally.
- **#1 parity release (PR #12 → merge 0e766cc):** develop→main; `git log main..develop` empty = full parity. Scoped to EXCLUDE PR #9 (kept unreviewed feature off prod). Confirmed main was already at PR #11 (849bf36); local origin/main ref had been stale, fetched to verify.
- **#2 PR #9 (3ec14ad):** branch was stale + conflicted on today's rewritten files. Instead of a messy merge, integrated only the real feature — new use-auto-sync.ts (native-only, throttled 2min, POSTs /api/sync/background on open+resume; already uses absolute APP_URL) + 2-line layout wiring — cleanly onto current develop. Compiles. Closed PR #9 (superseded, branch deleted).
- **Delivery:** Codemagic build #12 (3ec14ad) triggered → TestFlight, carries #4 + auto-sync to device.
**What did NOT work:** Codemagic "Degraded" (builds still run). Classifier blocks gh merge-to-main / compound gh — used bare single commands; verified via git fetch + Vercel deploy.
**Artifacts:** commits 36c3e02, 3ec14ad; PR #12 merged (0e766cc); PR #9 closed. main==develop at release time. Codemagic build #12 pending.
**Next / open:** Confirm build #12 on device (clean sign-out→relogin; auto-sync fires on open/resume). NOTE: auto-sync (3ec14ad) is now develop-only → main behind by it; fold into next release when ready (native-guarded, so web unaffected regardless). android/GH-Actions CI still unverified. This closes the mobile launch epic.

### [02:30] Green accent migration (#a1fc2a) + mobile theme unified, released to prod (branch: develop→main · commits: 772604f, 612b6cf · PR #13 merge 8a7d8ce · TF build 13)
**Synopsis:** User: mobile showed old blue, web showed new green — migrate mobile to the HAL palette and change the accent to #a1fc2a. Done + released to production (they need prod because X OAuth always redirects to prod, so a preview couldn't show the logged-in dashboard).
**What worked:** Two design systems existed — web dashboard uses packages/ui/hal (`--hal-a`, was #d4ff3a green) via `@helloagain/ui-hal/styles` (gated on `data-hal="on"`, set by dashboard/layout.tsx); mobile hardcoded #00d4ff blue inline + old apps/web/src/app/globals.css (also blue). Migration (commit 772604f): set accent #a1fc2a in BOTH systems (packages/ui/hal globals.css + theme.ts; and recolored old globals.css --accent-cyan + added --accent-rgb, routing glass/glow/selection/scrollbar through it); re-tinted all 10 mobile screens + MobileShareSheet from hardcoded #00d4ff/#0ea5e9 → var(--accent-cyan) and rgba(0,212,255,x) → rgba(var(--accent-rgb),x) via perl. Mobile reads the ungated :root vars — NO data-hal needed (avoided that fragility). Left bg/text shades as-is (already a matching dark theme). Evidence: mobile export compiled+synced clean. Released develop→main (PR #13 → 8a7d8ce, parity confirmed); prod web auto-redeployed green; Codemagic build #13 triggered for mobile.
**What did NOT work:** Vercel develop preview couldn't show the dashboard green (X OAuth redirects to prod), so preview-first evaluation was moot — went straight to prod per user.
**Artifacts:** commits 772604f (14 files), 612b6cf (journal); PR #13 merged. To retune the green later: --accent-cyan/--accent-rgb in apps/web/src/app/globals.css (mobile+landing) + --hal-a in packages/ui/hal (dashboard) — two places, same value.
**Next / open:** Confirm build #13 shows green on device + prod web shows #a1fc2a. A full component-level HAL migration of mobile (using ui-hal components, not just palette) remains a larger future option if desired. main==develop.

### [03:30] Mobile polish: viewport zoom/fit, residual blue, tap-to-open-X, search (develop→main · commit d3c08c2 · PR #14 merge 6253e4b · TF build 14)
**Synopsis:** User reported 5 mobile issues on the green build; fixed all, released to prod, rebuilt.
**What worked:**
- **Viewport (root layout.tsx `export const viewport`):** maximumScale=1 + userScalable=false stops the iOS input-focus zoom that shifted the app off-screen (root cause of both "tap field zooms" AND "content doesn't fit vertically"); viewportFit=cover + env(safe-area-inset-*) padding on mobile/layout.tsx `<main>` (top+bottom) & nav (bottom) & onboarding page. NOTE viewport is root-level → applies to web too (pinch-zoom off; web is desktop-first so negligible; can't scope to /mobile since that layout is 'use client' and viewport export needs a server component).
- **Residual blue → green:** HalLogo (packages/ui/src/HalLogo.tsx, #00d4ff→#a1fc2a) — @helloagain/ui resolves from dist, so rebuilt the package (verified a1fc2a in dist + mobile bundle); mobile-callback button (under /auth, missed by the earlier /mobile retint) → var(--accent-cyan).
- **Tap bookmark → open X:** home + bookmarks cards `window.open('https://x.com/<handle>/status/<x_post_id>', '_system')` (universal link → X app if installed else browser). Added x_post_id to home's Bookmark interface. Bookmarks card guards `if (swipedId===bm.id) return` so a swipe-to-delete doesn't also open X.
- **Search:** the mobile page hit the LIST endpoint with `?q=` which Zod-strips → no filtering. Route non-empty queries to /api/bookmarks/search (ranked FTS RPC, same response shape); also send tag filter as `tag_id` (schema name) not `tag` (was silently stripped → tag filter never worked either).
Evidence: @helloagain/ui rebuilt, mobile export compiled+synced clean, green present in bundle, no #00d4ff left in mobile/callback/HalLogo. Released develop→main (PR #14 → 6253e4b, parity) → prod web redeploy; Codemagic build #14 (d3c08c2) triggered.
**What did NOT work:** (none) — mobile-callback is under /auth so mobile:build moves it out (not compiled in the mobile export); it's a trivial string change verified by reasoning + will compile in the Vercel web build.
**Next / open:** Confirm build #14 on device: no zoom on field focus, content fits, green logo + callback, tapping a post opens X, search filters. App ICON (home-screen blue H) is a separate static asset — NOT changed (would need icon regeneration); flag if user wants it green too. main==develop.

### [04:50] Sync Now fix — GET→POST + honest X-error reporting (develop→main · commit 3114da3 · PR #15 merge e752ab3 · TF build 15)
**Synopsis:** User: "Sync Now fails." Diagnosed from prod logs: `GET /api/sync/background 405` (button used authFetch GET; route is POST-only) AND `POST … 200 [Sync] X API error 402: credits depleted` (X developer account out of credits → sync is a no-op, route swallowed it and returned 200).
**What worked:** Settings triggerSync GET→POST. Route (syncUser) now captures the X error status into the SyncResult (`xApiError`) instead of only console.error+break; POST returns it. Settings shows honest status: 402 → "X API quota reached — add credits to your X developer plan"; else "Synced N new"/"Up to date". Evidence: plain web build (`next build`, not mobile — includes /api so it type-checks the route) compiled 50/50 pages. Released develop→main (PR #15 → e752ab3, parity); Codemagic build #15 (3114da3) triggered.
**What did NOT work / ROOT CAUSE:** The X API account is **out of credits (402 Payment Required)** — sync cannot pull bookmarks until the user resolves X API billing/plan. This is external (X developer account), NOT a code issue. My fix only makes it VISIBLE + fixes the 405.
**Artifacts:** commit 3114da3 (route + settings); PR #15 merged. Codemagic build #15 pending.
**Next / open:** User must add credits / upgrade their X API plan for sync to actually import (the 402). After build #15: Sync Now will say "X API quota reached" (honest) until then. Also still open: home-screen app ICON is a blue static asset (not regenerated). main==develop.
