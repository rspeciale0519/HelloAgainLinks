# 2026-07-28

### [00:12] Dashboard "Recent" showed ingest time, not post date (branch: develop → main · commits 1e02cc3, 551e021 · PR #45)
**Synopsis:** User reported the web app still showing "when the bookmarks were imported rather than the dates the posts were originally posted". Traced to one call site on the dashboard home; fixed, released to prod, verified live.

**What worked + Evidence:**
- **Checked the data before the code.** Pulled the live `/api/bookmarks` response out of the CDT browser's network log (reqid 33) rather than trusting the UI: `post_created_at` was **correct and populated** on every row (`@coinbureau` = 2026-05-22, `bookmarked_at` = 2026-07-26). That immediately ruled out the ingest pipeline and the `|| new Date()` fallbacks in `batch/route.ts:58` and `background.ts:247` — the defect was purely presentational.
- **Swept every surface instead of fixing the reported one.** `grep bookmarked_at **/*.tsx` across `apps/web/src` returned 5 render sites. Four already label the ingest time explicitly ("Posted … / Saved 1d ago", "Added", "SAVED"); only `dashboard/page.tsx:344` rendered a bare `timeAgo(bookmark.bookmarked_at)` with no label, so it read as the post's age. Both mobile lists and the bookmarks page were already correct — screenshotted the bookmarks page to confirm rather than assuming.
- Fix mirrors the surfaces that were right: `formatPostDate(post_created_at || bookmarked_at, { short: true })`, saved time demoted to the `title` tooltip so no information is lost.
- Evidence: `npx tsc --noEmit` clean for `apps/web`; Vercel prod `dpl_Em83y1Pyve…` READY on `551e021`; **manual: reloaded prod and confirmed the @coinbureau row now reads `1:51AM - 5/22/26` where it previously read `1d ago`**, matching its stored `post_created_at`.

**What did NOT work:**
- **`mcp__postgres__query` points at local Supabase (127.0.0.1:54322), which is not running** — ECONNREFUSED. Prod DB inspection has to go through the browser (dashboard SQL editor or an authenticated in-page fetch); noted so the next session doesn't retry it.
- **The `sb-…-auth-token` value in `localStorage` is stale** (expired 2026-05-03) while the live session rides the chunked `.0`/`.1` **cookies**. Reading the token from localStorage to call the API yields a confident-looking 401. Reading the `authorization` header off an existing network request is the reliable route.
- **Used PowerShell here-string syntax (`@'…'@`) inside the Bash tool, twice** — for `git commit -m` and `gh pr create --body`. Bash takes it literally, so the commit subject became a bare `@`. Repaired with `commit --amend` + `push --force-with-lease` (safe: branch was seconds old and single-author) and `gh pr edit --body`. In the Bash tool use a `<<'EOF'` heredoc.

**Artifacts:** `apps/web/src/app/dashboard/page.tsx` (+7/-2). PR #45 develop→main. Prod at `551e021`.

**Next / open:** (1) **Mobile still shows the old behaviour** — its timestamp fix shipped in #39/#40 but the native bundle predates it; needs the Codemagic build off `develop` the user is running manually. (2) User still needs to reload extension 0.5.4 and run **REBUILD ORDER** to repair `bookmarked_at` on the ~1,600 pre-fix rows — note this is orthogonal to today's fix, which reads `post_created_at`. (3) Unchanged: Codemagic auto-trigger, expansions A/B on a fresh UTC day, Max tier absent from `UpgradePrompt.tsx` and mobile settings.
