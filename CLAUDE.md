

<!-- brain:pointer -->
## hal Brain (halbrain/) — mandatory protocol

A self-improving engineering memory lives in `halbrain/`. Hooks enforce it
(SessionStart injects state; Stop gates a per-task journal entry). State what we
last did before new work; on task completion append a journal entry per
`halbrain/CLAUDE.md`. NO secrets/PII. Consolidation after the user task only.
<!-- /brain:pointer -->

